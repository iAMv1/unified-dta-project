[pytest]
minversion = 7.0
addopts = -q

# Only collect tests from the consolidated tests directory
testpaths =
    tests

# Ensure the src/ path is available during test discovery
pythonpath =
    src

